node_modules/
dist/
.env
.env.local
*.tsbuildinfo
.DS_Store

# Rust build output. routing-core/pkg/ is deliberately NOT ignored:
# it is the built WASM artifact the server loads at runtime, committed by
# the build-wasm workflow because Render has no Rust toolchain.
routing-core/target/
